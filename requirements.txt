# 核心依賴
pytest>=7.0.0
pytest-asyncio>=0.21.0

# 選用 - 視覺化介面
rich>=13.0.0        # Terminal UI
textual>=0.40.0     # 進階 Terminal UI
gradio>=4.0.0       # Web UI (選用)
streamlit>=1.28.0   # Web UI (選用)

# 選用 - 開發工具
black>=23.0.0
ruff>=0.1.0
mypy>=1.0.0
